# Delete all unused properties.

# Give your module a name
name: zmk-keyboard-tanpa
build:
  # List your dependencies
  #depends:
  #  - zmk-example-dependency
  # Folder containing a "CMakeLists.txt" (. = root)
  #cmake: .
  # Kconfig filepath
  # kconfig: Kconfig
  settings:
    # Folder containing a "boards" folder (. = root)
    board_root: .
